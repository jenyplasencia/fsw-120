import React from "react"
import ReactDOM from "react-dom"

function Button(){
    return (
        
            <button className ="button"> OLDER POST - </button>
        )
}
ReactDOM.render( <Button />, document.getElementById("root"))
export default Button